# Success-Criteria Measurability Requirements Quality Checklist: SPEC 002

**Purpose**: Formal pre-`/speckit-tasks` gate — validate that the Success Criteria
(SC-001…SC-038), the human-evaluation protocol, and the objectivized subjective
terms (FR-021, FR-027, FR-029, FR-030, FR-042) in the SPEC 002 spec are
objectively verifiable, adequately instrumented, correctly sampled, traceable,
and free of coverage gaps before tasks are derived.
**Created**: 2026-09-08
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [quickstart.md](../quickstart.md)

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

**Evaluation history**:
- **2026-09-08 (pass 1)**: assessed against spec/plan/research/data-model/contracts/quickstart. 18 satisfied, 20 gaps (4 HIGH, 11 MEDIUM, 5 LOW). Gate: **CERRADA**.
- **2026-09-08 (pass 2, after remediation)**: spec.md added SC-031…SC-038, an objective "divergencia" threshold in SC-019, and a normative "Evaluación con personas" section (panel screening, literal prompts, SC-007 rubric, observable "expresa duda", shown views, timing table for SC-002/SC-008/SC-010, blocking re-test policy). plan.md, quickstart.md, research.md, data-model.md and contracts/visual-system.md, contracts/navigation-and-routes.md, contracts/auth-and-session.md updated for traceability + a new `visual-criteria.test.tsx` suite. **35 satisfied, 3 gaps (0 HIGH, 0 MEDIUM, 3 LOW).** Gate: **CERRADA — sin hallazgos HIGH; los 3 LOW son residuales y no bloquean.**

## Measurability & Objectivity of Success Criteria

- [x] CHK001 Are the pass conditions for the human-judged outcome criteria (SC-006, SC-007, SC-009, SC-014, SC-017, SC-019) expressed as objective graded rules, rather than relying on an unspecified grader's interpretation of terms like "el mismo producto", "herramienta técnica", "conserva la identidad" or "cero divergencias"? [Measurability, Spec §SC-006, §SC-007, §SC-019]
  - PASS (pass 2): spec §"Evaluación con personas" now gives objective rubrics for SC-006 (P1 = "IDAF"), SC-007 (three conditions with domain/function word lists + explicit "plantilla administrativa genérica" and "sin terminar" definitions) and SC-009 (observable behaviours (a)–(c)). SC-019 carries an objective "divergencia" threshold (63 (área, aspecto) pairs against the shared token/component/rule). SC-014 (contracts/visual-system.md §3.6) and SC-017 (navigation-and-routes.md §4 landmarks) were already objective.

- [ ] CHK002 Is "comprensible" (as used in "mensaje comprensible") given an objective acceptance definition where SCs depend on it (SC-002, SC-003), or does it remain a subjective judgement? [Ambiguity, Spec §SC-003]
  - GAP (LOW, residual — non-blocking): "comprensible" is still not operationalized. Not on the remediation scope. Mitigated as before: SC-003's load-bearing clause ("no indica cuál campo falló") is tested and the message is pinned to the `idaf.auth.invalidCredentials` content constant (contracts/auth-and-session.md §F4).

- [x] CHK003 For the negative outcome in SC-007 ("ninguno la describe como plantilla administrativa genérica o prototipo sin terminar"), is there an objective rule for classifying an evaluator's free-text description as a violation? [Measurability, Spec §SC-007]
  - PASS (pass 2): spec §"Evaluación con personas" → "Rúbrica de SC-007" defines valid answers (domain word ∧ function word from fixed lists), the "inválida por genérica" set ("un panel de administración", "un CRM", "un backoffice", "una plantilla de dashboard", "un sistema de gestión" a secas, "una app de administración de usuarios", or anything indistinguishable from a generic admin panel), the "no sin terminar" rule (module "no disponible" ≠ "sin terminar"), plus "Registro de dudas y respuestas ambiguas" (verbatim transcription, one neutral re-ask, ambiguity → fail, two independent reviewers). Mirrored in quickstart E7.

- [ ] CHK004 Is SC-018 ("región de mayor superficie") backed by a defined measurement method — viewport, the width at which it is measured, area computation, and tolerance? [Measurability, Spec §SC-018, §FR-030]
  - GAP (LOW, residual — non-blocking): method still delegated to `structure-consistency.spec.ts` (quickstart E9.2). Not on the remediation scope. "main is the largest region" via bounding-box area is near-self-evident; the missing explicit viewport/tolerance is cosmetic.

- [x] CHK005 Does SC-014's "cero elementos simulados" enumerate what counts as a simulated element (e.g., a disabled-looking control, an illustrative empty state)? [Clarity, Spec §SC-014, §FR-044]
  - PASS: contracts/visual-system.md §3.6 enumerates the prohibited set; FR-030 bars decorative imagery; `module-unavailable.test.tsx` enforces it. Controls are outright prohibited, so no disabled-looking controls exist.

- [x] CHK006 Is "utilizable" / "utilizables" defined with observable criteria where SCs turn on it (SC-012 icon hidden, SC-021 reduced width, SC-022 keyboard)? [Ambiguity, Spec §SC-012, §SC-021]
  - PASS (residual softness noted): SC-022 fully operationalized (quickstart E10 + auth-and-session §F8 + navigation-and-routes N6). SC-012 → navigation-and-routes N2 / quickstart E6.6 (link keeps its text accessible name and stays activable). SC-021 → quickstart E12.3 / navigation-and-routes §4 (zones visible and not collapsed at 1024 px) — softest of the three but observable.

## Verification Method & Instrumentation

- [x] CHK007 Does the spec (or a referenced artifact) define who runs the non-human SCs and by what method (automated E2E vs manual script)? [Gap, Spec §Success Criteria]
  - PASS: quickstart §4 maps every SC to a named Vitest/Playwright suite; §6 repeats method per SC (E1–E15 + "e2e" / "manual"). New SC-031…SC-038 mapped to `visual-criteria.test.tsx`, `structure-consistency.spec.ts`, `nav-icons-active.spec.ts`, `product-identity.spec.ts`, `primary-nav.test.tsx`, `registry.test.ts`, `tokens.test.ts`.

- [x] CHK008 For the timed criteria (SC-002 < 30 s, SC-008 < 5 s, SC-010 ≤ 3 s), is the measurement start event and stop event defined for each? [Clarity, Spec §SC-002, §SC-008, §SC-010]
  - PASS (pass 2): spec §"Evaluación con personas" → "Medición de tiempos" table gives explicit t0/t1 for all three; SC-002/SC-008/SC-010 bullets cross-reference it; quickstart E2.3, E4.1, E6.4 restate the events.

- [x] CHK009 For the timed criteria, is the measuring instrument and precision specified? [Measurability, Spec §SC-010]
  - PASS (pass 2): "Medición de tiempos" table — SC-002 = test-runner timestamp; SC-008 and SC-010 = digital stopwatch at 0,1 s resolution measured on the screen recording in post-review (removes facilitator reaction error).

- [x] CHK010 Is the "simulación de percepción sin color" in SC-015 specified as to type and tool? [Clarity, Spec §SC-015, §FR-021, §FR-039]
  - PASS: quickstart E13.2 — Chrome DevTools → Rendering → Emulate vision deficiencies → Achromatopsia. SC-031 now also anchors to this same simulation.

- [x] CHK011 Does SC-016 bound "componentes de interfaz esenciales" with an enumerated list? [Clarity, Spec §SC-016, §FR-040]
  - PASS: FR-040 enumerates the pairs; contracts/visual-system.md §1.4 tabulates each with its minimum ratio.

- [x] CHK012 Is the contrast-measurement procedure for SC-016 defined (state pairs, sampling, tooling)? [Consistency, Spec §SC-016, §FR-040]
  - PASS: contracts/visual-system.md §1.4 + quickstart E13.1 (axe-core or contrast checker, Login + 7 areas, pairs from §1.4). FR-030/SC-032 forbid decorative imagery, removing the foreground-over-image sampling problem.

- [x] CHK013 Does SC-025 define the repository/bundle inspection procedure? [Measurability, Spec §SC-025, §FR-055]
  - PASS (patterns delegated to tests): quickstart E14.1 defines artifacts (`dist/` from `npm run build`), steps (`no-frontend-secrets` unit + e2e, `git ls-files`) and scope; contracts/auth-and-session.md §B6 / §A5.4 confirm.

- [x] CHK014 Does SC-026 enumerate the "registros observables" in scope? [Clarity, Spec §SC-026, §FR-055]
  - PASS: SC-026 enumerates "consola del navegador y salida del mecanismo de autenticación"; quickstart E14.2 matches; the login `POST` body is the single sanctioned credential transport (data-model.md §1).

- [x] CHK015 Is the cross-area review in SC-019 given a defined procedure and a definition of what constitutes one "divergencia"? [Measurability, Spec §SC-019, §FR-036]
  - PASS (pass 2): SC-019 now states the objective threshold — for each of the 9 aspects, "same treatment" = produced by the same `tokens.css` token, the same shared component or the same `base.css` rule; one divergence = one (área, aspecto) pair resolved by a different token/component or a hard-coded bypass; pass = 0 over the 7 × 9 = 63 pairs. Procedure in quickstart E9.3 (style inspector + literal-value search); also automated in `structure-consistency.spec.ts`. Residual: the human reviewer role is still implicit, but the check is now mechanical.

- [x] CHK016 Is the method for verifying "zonas separadas" in SC-017 defined? [Consistency, Spec §SC-017, §SC-022]
  - PASS: navigation-and-routes.md §4 maps the four zones to landmarks; quickstart E9.1 + E10.2 assert presence and distinctness; `structure-consistency` + `keyboard-a11y` specs cover it.

## Sampling, Thresholds & Statistical Validity

- [ ] CHK017 For every "100 % de las veces / de los intentos" criterion, is the number of trials that constitutes the sample defined? [Gap, Spec §SC-001]
  - GAP (LOW, residual — non-blocking): deterministic SCs run once per browser × 3 (quickstart §4/§6), which is authoritative for a deterministic assertion; the flake-prone case (rapid alternation) is pinned at ≥ 20 (navigation-and-routes.md R11). No general "N attempts" for the "100 %" phrasing; cosmetic for deterministic checks. Not on the remediation scope.

- [x] CHK018 Given the panel is "al menos 5" with no upper bound, is it defined how the 100 % / 90 % thresholds are computed as panel size varies? [Ambiguity, Spec §SC-013, §Evaluación con personas]
  - PASS: minimum panel size fixed (≥ 5); thresholds are proportions of the vigente panel (100 % = every evaluator; SC-013 = mean across icons). Pass 2 also adds a per-icon floor.

- [x] CHK019 Is retest/remediation behaviour defined when a "100 %" or "cero" criterion fails on a single trial or evaluator? [Gap, Spec §SC-006, §SC-011]
  - PASS (pass 2): spec §"Evaluación con personas" → "Política de re-test de criterios bloqueantes" — for SC-006/SC-007/SC-009, one evaluator's failure ⇒ fix the root cause in the product (not the protocol) and re-run with a fresh screened panel; no "por mayoría"; max 2 re-tests, then documented owner decision. SC-008/SC-010/SC-013 ("juicio") → refinement task, no fresh-panel re-test. Restated in quickstart E7.6 and §6.

- [x] CHK020 For SC-013, does "al menos el 90 % … promediado por icono" also impose a per-icon or per-evaluator floor? [Ambiguity, Spec §SC-013]
  - PASS (pass 2): "Rúbrica de SC-013" now requires mean-per-icon ≥ 0,90 **and** no single icon below 0,60 (per-icon floor) **and** ≥ 80 % of the panel answering "Sí" to P8 (same visual family). SC-013 bullet and quickstart E6.5 updated.

- [x] CHK021 Are the magnitudes in SC-020 (≥ 40 / ≥ 200 characters) justified as representative, or arbitrary? [Clarity, Spec §SC-020, §FR-045, §FR-047]
  - PASS: FR-045/FR-047 delegate the amount as "más largo de lo habitual"; SC-020 pins concrete testable over-limit values exercised by quickstart §2 + E12 — adequate as intentional stress bounds.

- [x] CHK022 Is the "alternancia rápida" rate behind SC-011 / SC-023 quantified? [Traceability, Spec §SC-011, §SC-023]
  - PASS at plan level: research D8 + navigation-and-routes.md R11 pin ≥ 20; quickstart E6.3 uses ~25; SC-031 (pass 2) also states "≥ 20 alternancias".

## Human-Evaluation Protocol Quality

- [x] CHK023 Does the protocol define evaluator screening/exclusion beyond "perfil técnico … ajenos al diseño"? [Completeness, Spec §Evaluación con personas]
  - PASS (pass 2): "Panel y cribado de evaluadores" — ≥ 5; technical profile (network/infra ops or equivalent: SRE, network support, IoT engineering); no participation in design/spec/implementation of SPEC 002; **and, for SC-006/SC-007, no prior exposure to IDAF or the SPEC 001/002 prototypes**. Prior exposure allowed for SC-009/SC-010/SC-013. Restated in quickstart E7.

- [x] CHK024 Is the exact wording and order of the prompts fixed for SC-006/007/009/010/013? [Clarity, Spec §Evaluación con personas]
  - PASS (pass 2): "Preguntas literales (se leen textualmente, sin variación ni sinónimos)" — P1–P8 verbatim, fixed order, plus the fixed material order Login → Inicio → Topología. Mirrored in quickstart E7.2.

- [x] CHK025 For SC-009, is "expresa duda" given an observable definition? [Measurability, Spec §SC-009]
  - PASS (pass 2): "Rúbrica de SC-009" — fail if any evaluator: (a) uses/points/hovers a **module content-area** control as a logout attempt, (b) verbalises uncertainty about which control to use (example phrases listed), or (c) takes > 10 s to head to the header "Cerrar sesión". Utterances and first control touched are transcribed verbatim.

- [x] CHK026 Does the protocol specify which internal view(s) are shown for SC-006/SC-007? [Completeness, Spec §SC-006, §FR-028]
  - PASS (pass 2): "Material mostrado" — Login, Inicio, **and a "No disponible" module view (Topología)** in fixed order, so identity "en cualquier módulo" (FR-028 / US2 AS2) is exercised by the panel, not only structurally by SC-014.

- [x] CHK027 Is it defined whether the human-evaluation criteria are blocking? [Ambiguity, Spec §Assumptions]
  - PASS: quickstart §6 "Bloqueante" column — SC-006/007/009 blocking; SC-008/010/013 non-blocking ("juicio") with mandatory execution + logging. Pass 2 adds the blocking re-test policy in spec + quickstart §6.

## Objectivized Subjective Terms (FR anchors)

- [x] CHK028 Is FR-021's ≥2-signal / ≥1-non-chromatic rule verified by a dedicated success criterion? [Coverage, Gap; Spec §FR-021]
  - PASS (pass 2): **SC-031** added — exactly one active entry per internal view (zero views with none or >1), active entry differs from inactive in ≥ 2 rendered properties of which ≥ 1 is non-chromatic and stays perceptible under achromatopsia; no exceptions on ≥ 20 rapid alternations. Anchored in contracts/visual-system.md §4/VS-3 and navigation-and-routes.md N4/N5; tests `primary-nav.test.tsx` + `nav-icons-active.spec.ts`.

- [x] CHK029 Is FR-027's composite "evolución" (a)–(e) matched by an objective success criterion? [Coverage, Gap; Spec §FR-027]
  - PASS (pass 2): **SC-036** added — the result presents jointly and verifiably all five of FR-027 (a)–(e); fails if any is missing; verified by the union of SC-006/SC-013/SC-016/SC-017/SC-019 plus a structural check (`product-identity.spec.ts`; contracts/visual-system.md §5 / VS-13; quickstart E7/E9).

- [x] CHK030 Are FR-029's objective login-screen conditions reflected in a measurable success criterion? [Coverage, Gap; Spec §FR-029]
  - PASS (pass 2): **SC-037** added — access screen contains name + brand symbol, purpose text, same button/error component + tokens as the internal app, zero decorative imagery, and the form is not styled differently. Anchored in contracts/auth-and-session.md §F7 + contracts/visual-system.md §5 / VS-14; tests `product-identity.spec.ts` + `login-view.test.tsx`.

- [x] CHK031 Are FR-030's objective criteria (no decorative imagery; animations ≤ 150 ms / no loop; line-height ≥ 1.4) each traceable to a success criterion? [Coverage, Gap; Spec §FR-030, §SC-018]
  - PASS (pass 2): **SC-032** (100 % of `<img>`/`background-image`/`<svg>`/`<canvas>` functional or absent — zero decorative graphics competing with functional info), **SC-033** (every transition/animation ≤ 150 ms, `animation-iteration-count` finite, zero loops), **SC-034** (content-text `line-height` ≥ 1,4). Tests: `visual-criteria.test.tsx` + `structure-consistency.spec.ts` + `tokens.test.ts`; contracts/visual-system.md §1.3 / VS-11; quickstart E15.

- [x] CHK032 Are FR-042's density limits covered by a measurable success criterion? [Coverage, Gap; Spec §FR-042]
  - PASS (pass 2): **SC-035** — `<h1>` computed font-size ≤ 2,0× body font-size; no continuous purposeless vertical gap > 1,5× the rendered `ModuleHeader` height; every interactive control hit target ≥ 24×24 px. Line-height ≥ 1,4 covered by SC-034. Tests: `visual-criteria.test.tsx` + `structure-consistency.spec.ts` + `tokens.test.ts`; contracts/visual-system.md §1.3 / VS-11; quickstart E15.

## Traceability & Consistency Between Criteria

- [x] CHK033 Does each success criterion identify the requirement(s) / user story it validates? [Traceability, Spec §Success Criteria]
  - PASS: SC-031…SC-038 each carry an explicit "(ancla FR-xxx)" tag in the SC list; the older SCs remain mapped in quickstart §4/§6, research "Resumen de decisiones", and the contracts' "criterios verificables" tables.

- [x] CHK034 Are SC-005 and SC-024 consistent and non-redundant? [Consistency, Spec §SC-005, §SC-024]
  - PASS: overlap without conflict; navigation-and-routes.md R7/R8/R9 disambiguate the three behaviours; data-model.md §4 transitions cover both.

- [x] CHK035 Do SC-021 and SC-022 (min width, three browsers) trace to a spec-level definition? [Dependency, Spec §SC-021, §SC-022]
  - PASS: width delegated + recorded in spec Assumptions ("fijado en 1024 px") and research D14; browsers Chromium/Firefox/WebKit in plan Technical Context + quickstart §1 (plan-level only, but concrete and consistent).

- [x] CHK036 Is there a success criterion for FR-025 (unique name + icon; no duplicate nav entries)? [Coverage, Gap; Spec §FR-025]
  - PASS (pass 2): **SC-038** added — each of the seven areas has exactly one navigation entry with a unique (label, icon) pair; zero duplicate entries; Inicio overview and primary navigation derive from `MODULE_REGISTRY`. Tests `registry.test.ts` + `primary-nav.test.tsx`; navigation-and-routes.md N1/§5; contracts/visual-system.md §5 / VS-2.

- [x] CHK037 Is there a success criterion tied to FR-038 (consistent semantic-colour meaning across modules)? [Coverage, Gap; Spec §FR-038]
  - PASS: contracts/visual-system.md §1.1 (V1) binds FR-038 to SC-019; the single-source `tokens.css` with no per-component literal colours makes per-module divergence structurally impossible; `tokens.test.ts` guards the set. SC-019's new objective divergence threshold (CHK015) sharpens this.

- [x] CHK038 Is the overall acceptance rule for the gate defined? [Gap, Spec §Success Criteria]
  - PASS: quickstart §6 tabulates all 38 SCs with Método + Bloqueante — the blocking SCs must pass; SC-008/SC-010/SC-013 non-blocking but run + logged, failures → refinement tasks; SC-031…SC-038 block like the rest. Blocking-panel re-test policy stated.

## Result Summary (pass 2 — 2026-09-08, after remediation)

**Satisfied (35):** CHK001, CHK003, CHK005, CHK006, CHK007, CHK008, CHK009, CHK010, CHK011, CHK012, CHK013, CHK014, CHK015, CHK016, CHK018, CHK019, CHK020, CHK021, CHK022, CHK023, CHK024, CHK025, CHK026, CHK027, CHK028, CHK029, CHK030, CHK031, CHK032, CHK033, CHK034, CHK035, CHK036, CHK037, CHK038

**Open gaps (3):** CHK002, CHK004, CHK017 — all **LOW / residual / non-blocking**, outside the remediation scope:
- CHK002 — "comprensible" never operationalized; SC-003's load-bearing clause is tested and the message is a fixed content constant.
- CHK004 — SC-018 "mayor superficie" measurement method delegated to the E2E (no explicit viewport/tolerance); assertion is near-self-evident.
- CHK017 — "100 %" phrasing carries no explicit trial count for inherently deterministic checks (automated ×3 browsers); rapid-alternation is pinned at ≥ 20.

### Findings by severity (pass 2)

- **HIGH: 0** (pass 1 had 4 — all resolved: SC-031/FR-021, SC-032+SC-033+SC-034/FR-030, SC-035/FR-042, SC-007 rubric).
- **MEDIUM: 0** (pass 1 had 11 — all resolved: panel screening, literal prompts, "expresa duda", shown views, blocking re-test policy, timing instrument, SC-019 threshold, FR-027→SC-036, FR-029→SC-037).
- **LOW: 3** (CHK002, CHK004, CHK017 — documented, non-blocking).

### Gate decision: **CERRADA (closed — passed)**

No HIGH findings remain (the pass-1 blockers are resolved by SC-031…SC-038, the objective "divergencia" threshold, and the normative "Evaluación con personas" section, with matching plan/quickstart/contract traceability and a new `visual-criteria.test.tsx` suite). The three remaining LOW items are residual, non-blocking, and were outside the requested remediation scope; they can be picked up opportunistically. Requirements-quality is sufficient for `/speckit-tasks`.

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- This checklist tests whether the success criteria are *well written and verifiable* — not whether the implementation meets them
- Companion checklists for this feature: `visual-ux.md`, `auth-session.md`, `accessibility.md`, `navigation.md`
